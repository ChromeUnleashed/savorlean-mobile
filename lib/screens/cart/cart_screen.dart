// lib/screens/cart/cart_screen.dart
// Shows the current cart contents with quantity controls, promo code field,
// order total, and a button to proceed to checkout.
// Fully implemented in Phase 3-1.

import 'package:flutter/material.dart';

/// Cart screen — Phase 3-1 stub.
class CartScreen extends StatelessWidget {
  const CartScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Cart Screen — Phase 3-1')));
  }
}
