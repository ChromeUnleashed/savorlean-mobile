// lib/screens/auth/login/login_screen.dart
// Email/password login screen with Google Sign-In option.
// After successful login, redirects back to the page the user came from.
// Fully implemented in Phase 1-4.

import 'package:flutter/material.dart';

/// Login screen — Phase 1-4 stub.
class LoginScreen extends StatelessWidget {
  const LoginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Sign In')),
      body: const Center(child: Text('Login Screen — Phase 1-4')),
    );
  }
}
