// lib/screens/auth/register/register_screen.dart
// New account registration screen — collects name, email, and password.
// Fully implemented in Phase 1-4.

import 'package:flutter/material.dart';

/// Register screen — Phase 1-4 stub.
class RegisterScreen extends StatelessWidget {
  const RegisterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Create Account')),
      body: const Center(child: Text('Register Screen — Phase 1-4')),
    );
  }
}
