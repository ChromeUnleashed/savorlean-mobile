// lib/screens/auth/forgot_password/forgot_password_screen.dart
// Password reset screen — user enters their email and Supabase sends a reset link.
// Fully implemented in Phase 1-4.

import 'package:flutter/material.dart';

/// Forgot password screen — Phase 1-4 stub.
class ForgotPasswordScreen extends StatelessWidget {
  const ForgotPasswordScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Reset Password')),
      body: const Center(child: Text('Forgot Password Screen — Phase 1-4')),
    );
  }
}
