// lib/screens/account/wishlist/wishlist_screen.dart
// Shows all meals the user has saved to their wishlist.
// Supports optimistic removal with an X button on each card.
// Fully implemented in Phase 4-3.

import 'package:flutter/material.dart';

/// Wishlist screen — Phase 4-3 stub.
class WishlistScreen extends StatelessWidget {
  const WishlistScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('My Wishlist')),
      body: const Center(child: Text('Wishlist Screen — Phase 4-3')),
    );
  }
}
