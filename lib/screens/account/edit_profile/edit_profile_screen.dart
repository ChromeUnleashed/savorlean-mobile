// lib/screens/account/edit_profile/edit_profile_screen.dart
// Allows the user to update their display name and phone number.
// Saves changes to the Supabase profiles table.
// Fully implemented in Phase 4-4.

import 'package:flutter/material.dart';

/// Edit profile screen — Phase 4-4 stub.
class EditProfileScreen extends StatelessWidget {
  const EditProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Edit Profile')),
      body: const Center(child: Text('Edit Profile — Phase 4-4')),
    );
  }
}
