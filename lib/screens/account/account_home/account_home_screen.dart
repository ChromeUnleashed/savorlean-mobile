// lib/screens/account/account_home/account_home_screen.dart
// Account hub screen — shows the user's name/email and navigation tiles
// for Orders, Wishlist, Edit Profile, and Sign Out.
// Fully implemented in Phase 4-1.

import 'package:flutter/material.dart';

/// Account home screen — Phase 4-1 stub.
class AccountHomeScreen extends StatelessWidget {
  const AccountHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Account Screen — Phase 4-1')),
    );
  }
}
