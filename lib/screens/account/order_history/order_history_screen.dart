// lib/screens/account/order_history/order_history_screen.dart
// Lists all past orders for the signed-in user.
// Shows date, total, and status badge for each order.
// Fully implemented in Phase 4-2.

import 'package:flutter/material.dart';

/// Order history screen — Phase 4-2 stub.
class OrderHistoryScreen extends StatelessWidget {
  const OrderHistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('My Orders')),
      body: const Center(child: Text('Order History — Phase 4-2')),
    );
  }
}
