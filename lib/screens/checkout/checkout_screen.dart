// lib/screens/checkout/checkout_screen.dart
// Collects delivery details and places the order via Supabase.
// Shows delivery name, phone, address, meal instructions, order summary,
// and a "Place Order" button.
// Fully implemented in Phase 3-3.

import 'package:flutter/material.dart';

/// Checkout screen — Phase 3-3 stub.
class CheckoutScreen extends StatelessWidget {
  const CheckoutScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Checkout')),
      body: const Center(child: Text('Checkout Screen — Phase 3-3')),
    );
  }
}
