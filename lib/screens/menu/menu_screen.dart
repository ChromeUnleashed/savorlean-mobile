// lib/screens/menu/menu_screen.dart
// The menu browsing screen.
// Shows a category filter bar and a grid of MealCard widgets.
// Fully implemented in Phase 2-2.

import 'package:flutter/material.dart';

/// Menu screen — Phase 2-2 stub.
class MenuScreen extends StatelessWidget {
  const MenuScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Menu Screen — Phase 2-2')));
  }
}
