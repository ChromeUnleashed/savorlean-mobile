// lib/screens/plans/plans_screen.dart
// Lists all available subscription meal plans.
// Fully implemented in Phase 2-4.

import 'package:flutter/material.dart';

/// Plans screen — Phase 2-4 stub.
class PlansScreen extends StatelessWidget {
  const PlansScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Meal Plans')),
      body: const Center(child: Text('Plans Screen — Phase 2-4')),
    );
  }
}
